services:
  backend:
    image: parkingcontroller-backend:test
    container_name: parking_backend_vm
    restart: always
    environment:
      DATABASE_URL: postgresql://postgres:a.123456@192.168.122.126:5433/db_parking
      SECRET_KEY: valor_de_prueba
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 1440
      BACKEND_CORS_ORIGINS: '["http://localhost:8089", "http://192.168.122.130:8089"]'
    ports:
      - "8000:8000"

  frontend:
    image: parkingcontroller-frontend:test
    container_name: parking_frontend_vm
    restart: always
    ports:
      - "8089:80"
